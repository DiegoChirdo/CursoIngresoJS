function mostrar()
{
//tomo la edad  
// Comparaciones lògicas: >, <, == (Comparar), != (No igual), && (Y), || (Conector lógico)

	var edad;
	edad = document.getElementById('edad').value;
	edad=parseInt(edad);

if(edad==15)
{
alert("Niña bonita");
};


}//FIN DE LA FUNCIÓN